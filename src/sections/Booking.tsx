"use client";

import React from "react";
import { InlineBookingCalendar } from "@/components/InlineBookingCalendar";

/** Homepage booking section — the shared inline calendar, in its original slot. */
export const Booking: React.FC = () => <InlineBookingCalendar />;
